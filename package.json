{
  "name": "viriato-terra",
  "private": true,
  "packageManager": "pnpm@11.9.0",
  "scripts": {
    "dev": "pnpm --parallel --filter web --filter backend dev",
    "dev:web": "pnpm --filter web dev",
    "dev:backend": "pnpm --filter backend dev",
    "build": "pnpm --recursive build",
    "lint": "pnpm --recursive lint",
    "typecheck": "pnpm --recursive typecheck",
    "test": "pnpm --recursive test",
    "db:up": "docker compose --env-file infra/postgres/.env -f infra/postgres/docker-compose.yml up -d",
    "db:down": "docker compose --env-file infra/postgres/.env -f infra/postgres/docker-compose.yml down",
    "db:logs": "docker compose --env-file infra/postgres/.env -f infra/postgres/docker-compose.yml logs -f",
    "db:migrate": "pnpm --filter backend payload migrate",
    "db:seed": "pnpm --filter backend seed"
  }
}
