POSTGRES_DB=viriato
POSTGRES_USER=viriato
POSTGRES_PASSWORD=change-me
# Use 0.0.0.0 only on a trusted LAN and restrict port 5432 with the firewall.
POSTGRES_BIND_ADDRESS=0.0.0.0
PGADMIN_DEFAULT_EMAIL=admin@example.com
PGADMIN_DEFAULT_PASSWORD=change-me
PGADMIN_BIND_ADDRESS=0.0.0.0
